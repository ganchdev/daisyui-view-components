<% if form.present? %>
  <%= form.file_field field, class: css(css_classes), **html_options.except(:class) %>
<% else %>
  <%= file_field_tag field, **html_options, class: css(css_classes), **html_options.except(:class) %>
<% end %>
